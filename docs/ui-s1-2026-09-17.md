# UI S1 状态路由与 profile 启动证据（2026-09-17）

## 判定

S1 的第一条可运行切片通过：Docker 目标 profile 在 `paper` 模式启动，认证后的只读
`GET /api/trade/state` 返回状态投影；没有新增交易控制路径。启动恢复步骤的持久化计数
与浏览器可视化仍属于后续工作。

## 可复现检查

| 检查 | 结果 |
|---|---|
| `pnpm verify` | ✅ 61 个测试文件 / 650 个测试通过 |
| `node scripts/client-s0-check.mjs` | ✅ client registration、`main`、`sidebar.panellist`、只读边界通过 |
| `dsh --profile trade --dump-config` | ✅ 12 个 host entry + 1 个 client carrier |
| `TRADER_MODE=paper dsh --profile trade --no-open --port 3099` | ✅ Web server 启动；本次验证后已停止 |
| 认证 `GET /api/trade/state` | ✅ HTTP 200；`mode=paper`、`venue=paper`、无持仓、无挂单 |

请求经过 DSH connection 的认证 cookie；证据中不记录 token、环境文件或任何密钥。
本次只读查询使用 paper broker，不触碰真实交易所。

## 关键实现

- `dsh-trader/plugins/ui` 注册精确 GET `/api/trade/state`，runtime 未就绪返回 503 + 原因。
- 状态投影使用 `exchange-live/exchange-stale/unknown` 可信度，重建中不返回旧值或 0。
- `trade-ui-client` 只负责浏览器 bundle；不读取 `connection`，不提供下单、撤单或模式切换。

## 尚未完成

1. 将 runtime 启动步骤、失败原因、近 1h/24h 重启次数持久化并接入状态路由。
2. client 面板消费 `/api/trade/state`，呈现当前状态与可信度标记。
3. 在实际浏览器中确认外壳、座位和 session/event 归属；当前只完成服务端 Web 启动与 HTTP 证据。
