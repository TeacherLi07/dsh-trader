# P1.5 通道有效性闸门 —— 首次可运行判定（2026-09-14）

> 由 `node scripts/ab-gate.mjs htx BTC/USDT 1h 92` 产出。原始输出另存
> `docs/p1.5-gate-run-2026-09-14.json`。判据来自 plan §10「P1.5 通道有效性闸门」。

## 这次运行证明了什么

1. **闸门本身是通的**：真实行情回补 → 两臂回放 → 配对 bootstrap → 套预注册判据 → 判定，
   全流程一次跑完，报告由代码渲染（`renderGateReport`），不存在手工挑指标的空间。
2. **判定是"关闭 W2/W3"**：这次运行**没有**证出判断通道的正贡献，而且样本太薄
   （配对逐笔 n=1），所以按 §10 的默认口径降级为「纯窗口 + 机械执行」——
   这正是闸门该做的事：**没有证据就不升级信任**。
3. **不能把这次运行当成 W2/W3 的最终判决**，原因有二（已写入 plan §12 第 20/21 项）：
   - B 臂用的是**确定性替身**（`standInJudge`）而不是 LLM 判断通道 —— 后者需要模型凭据；
   - 当前计划卡/规则配置在 92 天里只产生 16 次触发、1 笔配对成交，判断层几乎没有出手机会。

## 原始报告

```
# P1.5 通道有效性闸门（plan §10）
判断通道实现：stand-in（确定性替身，非 LLM；见 §12 第 20 项）

| 指标 | A（纯机械） | B（+判断通道） |
|---|---|---|
| 成交笔数 | 1 | 1 |
| 净 PnL (USD) | -19.1572 | -19.1572 |
| 最大回撤 (USD) | 19.1572 | 19.1572 |
| 执行偏离次数 | 0 | 0 |
| bar 数 | 2207 | 2207 |
| 触发次数 | 16 | 16 |
| 判断经手/通过/否决 | — | 2/2/0 |

配对差值 bootstrap：mean=0.0000，95% CI = [0.0000, 0.0000]，n=1，迭代=10000
回撤比 = 1.000×（上限 1.2×）

**结论无效（样本不足）**：
- 可配对逐笔 1 < 要求 20 —— bootstrap 在此样本量上退化
**未达标**：
- 净 PnL 差值 bootstrap 95% CI 下界 0.0000 ≤ 0

**判定：关闭 W2/W3，退化为「纯窗口 + 机械执行」（完整可用）。**
```
