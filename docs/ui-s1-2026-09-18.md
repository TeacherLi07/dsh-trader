# UI S1 启动恢复投影证据（2026-09-18）

## 判定

S1 功能闭环通过。启动过程现在写入已有 append-only `audit_events`，服务端从最近一次
`startup_boot` 重建步骤状态；runtime 尚未注册 ports 时，认证状态路由仍能返回这份启动
投影。没有新增数据库表，也没有新增交易写接口。

## 可复现检查

```bash
cd /workspace/dsh-trader
pnpm verify
node scripts/client-s0-check.mjs
```

单测覆盖非空启动样本：

- 最新启动仍在 `database` 时，投影为 `rebuilding`，不会提前显示 `ready`；
- 一次失败启动 + 一次成功启动后，近 1h/24h 启动计数均为 2；
- 失败原因逐字保留，步骤保留 `startedAt` / `finishedAt`；
- runtime paper 集成启动后，`boot/database/exchange/recovery/reconcile/ready` 全部为 `succeeded`。

## 当前边界

浏览器视觉验收按用户决定暂缓；client 已消费 `phase`、`currentStep`、`uptimeMs`、
`restartCount1h`、`restartCount24h` 和 `lastFailure`。状态面仍只有同源 GET，未加入下单、
撤单、模式切换或 watchdog。
